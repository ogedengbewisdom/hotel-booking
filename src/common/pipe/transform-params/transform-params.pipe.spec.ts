import { TransformParamsPipe } from './transform-params.pipe';

describe('TransformParamsPipe', () => {
  it('should be defined', () => {
    expect(new TransformParamsPipe()).toBeDefined();
  });
});
